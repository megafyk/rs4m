package com.rs4m.observer;

public class EventManager implements Publisher {

    @Override
    public void register(Subscriber subscriber) {
        // Implementation for registering a subscriber
    }

    @Override
    public void unregister(Subscriber subscriber) {
        // Implementation for unregistering a subscriber
    }

    @Override
    public void notify(Object event) {
        // Implementation for notifying subscribers about an event
    }
}
